<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="arira.co.id.mesinatrian">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1409435899101504~5421284897"/>

        <activity android:name="arira.co.id.mesinatrian.ui.activity.riwayat.RiwayatActivity" />
        <activity android:name=".ui.activity.dataBaru.DataActivity" />
        <activity android:name=".ui.activity.pengaturan.PengaturanActivity" />
        <activity android:name=".ui.activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.onboarding.OnBoardingActivity" />
        <activity android:name=".ui.activity.main.MainActivity" />
    </application>

</manifest>